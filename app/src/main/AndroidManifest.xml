<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Helper_for_conf">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.calculators.DiameterCalculatorActivity"
            android:theme="@style/Theme.Helper_for_conf" />
        <activity
            android:name=".activities.calculators.RatioCalculatorActivity"
            android:theme="@style/Theme.Helper_for_conf" />
        <activity
            android:name=".activities.recipes.RecipeListActivity"
            android:theme="@style/Theme.Helper_for_conf" />
        <activity
            android:name=".activities.recipes.RecipeDetailActivity"
            android:theme="@style/Theme.Helper_for_conf" />
        <activity
            android:name=".activities.recipes.RecipeEditActivity"
            android:theme="@style/Theme.Helper_for_conf" />
        <activity
            android:name=".activities.recipes.ChapterEditActivity"
            android:theme="@style/Theme.Helper_for_conf" />
        <activity
            android:name=".activities.recipes.IngredientEditActivity"
            android:theme="@style/Theme.Helper_for_conf" />
    </application>

</manifest>